@model Salvation.ViewModels.UsuarioViewModel
@* // A View recebe um UsuarioViewModel, que contém os dados necessários para criar um usuário. *@

@{
    ViewData["Title"] = "Cadastrar Usuário";
}

<h2>@ViewData["Title"]</h2>

<form asp-action="Create">
    <!-- Formulário que será enviado para a Action Create (POST) no UsuarioController. -->

    <div class="mb-3">
        <label asp-for="Nome" class="form-label"></label>
        <input asp-for="Nome" class="form-control bg-dark text-light" />
        <span asp-validation-for="Nome" class="text-danger"></span>
        <!-- asp-for liga o input à propriedade Nome do ViewModel.
             asp-validation-for mostra mensagens de validação (DataAnnotations). -->
    </div>

    <div class="mb-3">
        <label asp-for="Email" class="form-label"></label>
        <input asp-for="Email" class="form-control bg-dark text-light" />
        <span asp-validation-for="Email" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="Senha" class="form-label"></label>
        <input asp-for="Senha" class="form-control bg-dark text-light" type="password" />
        <span asp-validation-for="Senha" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="DataNascimento" class="form-label"></label>
        <input asp-for="DataNascimento" class="form-control bg-dark text-light" type="date" />
        <span asp-validation-for="DataNascimento" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="TipoUsuarioId" class="form-label bg-dark text-light"></label>
        <select asp-for="TipoUsuarioId" asp-items="Model.TiposUsuario" class="form-select bg-dark text-light">
            <option value="">Selecione</option>
        </select>
        <span asp-validation-for="TipoUsuarioId" class="text-danger"></span>
        <!-- asp-items popula o select com a lista TiposUsuario passada no ViewModel. -->
    </div>

    <button type="submit" class="btn btn-outline-primary">Salvar</button>
    <a asp-action="Index" class="btn btn-outline-secondary">Voltar</a>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <!-- Inclui os scripts de validação client-side (jQuery Validation + Unobtrusive). -->
}